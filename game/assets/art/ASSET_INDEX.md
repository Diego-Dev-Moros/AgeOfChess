# Age of Chess Runtime Art Assets

Godot loads assets from this folder with `res://assets/art/...`.

This is the runtime mirror of the repo-level `assets/art` folder. Keep gameplay-facing filenames stable so scripts can load cards, terrain boards, dice, and pieces by ID.
